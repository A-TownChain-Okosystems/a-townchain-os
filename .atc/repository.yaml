standard: ATC-STD-201
standard_version: "1.0.1"
version: "0.1.0"  # Kanonische Repo-Version (SSOT, SCR-0072) — README/Status leiten sich ab
repository:
  name: a-townchain-os
  classification: INFRA
  maturity: R3
  status: development
project:
  organization: A-TownChain-Okosystems
  domain: integration
  component: integration
language:
  primary: rust
license:
  type: apache-2.0
security:
  criticality: high
api:
  stability: experimental
protocol:
  compatibility: required
